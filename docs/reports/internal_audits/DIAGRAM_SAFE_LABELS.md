# Diagram Safe Labels

Use these replacements whenever the original diagram text would overclaim the current evidence.

| Original box                                                                   | Safer current label                                                                                      | Why                                                                                                                                                                        |
|:-------------------------------------------------------------------------------|:---------------------------------------------------------------------------------------------------------|:---------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Normal Data Generation OpenDSS Simulation (Voltage, Current, Power, SOC, Temp) | OpenDSS normal-operation simulation with electrical, DER, and control observables                        | Implemented, but the original label omits feeder losses, control states, cyber counts, and derived residuals.                                                              |
| Baseline Models ML Models (LSTM, Autoencoder, Isolation Forest)                | Benchmark model suite (Transformer, LSTM, GRU, Autoencoder, Isolation Forest, threshold, token baseline) | The model suite exists, but the original label omits Transformer and GRU and can misread the autoencoder as an LSTM autoencoder.                                           |
| LLM Analysis Context Learning of Normal Behavior                               | Optional context summaries for operator-facing interpretation                                            | Legacy context artifacts exist, but this is not part of the frozen canonical winner path.                                                                                  |
| Model Evaluation Reconstruction Loss, Threshold (p99), Accuracy                | Benchmark evaluation with threshold calibration and detection metrics                                    | Evaluation exists, but it is not universally reconstruction-loss-based and the benchmark emphasizes F1/AP/ROC rather than a single p99 accuracy label.                     |
| System Card Creation (Grid + Constraints + Physics Rules)                      | Scenario design with grid-aware constraints and validator rules                                          | Constraint-aware scenario construction is evidenced indirectly through bundle structure and validator outputs, but no standalone explicit system-card artifact is present. |
| LLM Scenario Generator Synthetic Anomaly / Zero-day Style                      | LLM-generated synthetic heldout attack scenarios                                                         | LLM-generated heldout bundles exist, but 'zero-day style' should not be inflated into real-world zero-day robustness.                                                      |
| Quality Check Plausibility + Diversity + Metadata                              | Validated scenario quality, diversity, and coverage audit                                                | Coverage, diversity, balance, plausibility, and difficulty are now backed by persisted tables and figures.                                                                 |
| Model Training ML Models + Tiny LLM (LoRA)                                     | Canonical ML benchmark plus experimental LoRA explanation branch                                         | Canonical ML benchmark is real and frozen; LoRA exists but remains an explanation-side extension, not the main detector.                                                   |
| Anomaly Detection Prediction Reconstruction Loss                               | Anomaly detection scoring across supervised and normal-oriented models                                   | Detection is implemented, but not all models are reconstruction-loss-based; the canonical winner is a supervised sequence classifier.                                      |
| Decision Layer Threshold + Context Reasoning                                   | Thresholded detection plus optional explanation/context support                                          | Thresholding is central; context reasoning exists only as an auxiliary explanation-side layer.                                                                             |
| Human-like Explanation LLM Generates Root Cause Analysis                       | Grounded explanation layer for post-alert operator support                                               | Family attribution is materially stronger than asset localization and grounding is partial, so human-like root-cause analysis is not supported.                            |
| Evaluation Metrics F1, Precision, Recall, AOI                                  | Detection metrics (F1, precision, recall, AP/ROC) plus explanation grounding metrics                     | F1/precision/recall are implemented; AOI is not a separately established repository metric and should not be claimed as such.                                              |
| Deployment Vision Lightweight Edge AI for DER                                  | Offline lightweight deployment benchmark for frozen detector packages                                    | Offline replay timing and footprint are measured on a workstation CPU only; no edge hardware or field deployment evidence exists.                                          |
